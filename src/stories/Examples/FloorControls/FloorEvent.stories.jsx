import React from "react";
import { FloorEvent } from "./FloorEvent";

export default {
  title: "Examples/FloorEvent",
  coment: FloorEvent,
};
const FloorEventLate = () => <FloorEvent />;
export const FloorEvents = FloorEventLate.bind({});
