import React from "react";

import { Sound } from "./03Sound";

export default {
  title: "Media/Sound",
  component: Sound,
};

const SoundLate = () => <Sound />;
export const SoundLates = SoundLate.bind({});
