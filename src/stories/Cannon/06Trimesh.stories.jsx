import React from "react";

import { Trimesh } from "./06Trimesh";

export default {
  title: "Cannon/Trimesh",
  component: Trimesh,
};

const TrimeshLate = () => <Trimesh />;
export const TrimeshLates = TrimeshLate.bind({});
