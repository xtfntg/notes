import React from "react";

import { ModelLoad } from "./07ModelLoad";

export default {
  title: "Basis/ModelLoad",
  component: ModelLoad,
};

const ModelLoadLate = () => <ModelLoad />;
export const ModelLoadLates = ModelLoadLate.bind({});
