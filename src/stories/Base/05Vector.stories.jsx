import React from "react";

import { Vector } from "./05Vector";

export default {
  title: "Basis/Vector",
  component: Vector,
};

const VectorLate = () => <Vector />;
export const VectorLates = VectorLate.bind({});
