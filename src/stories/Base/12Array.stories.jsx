import React from "react";

import { Array } from "./12Array";

export default {
  title: "Basis/Array",
  component: Array,
};

const ArrayLate = () => <Array />;
export const ArrayLates = ArrayLate.bind({});
