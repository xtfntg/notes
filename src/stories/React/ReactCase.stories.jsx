import React from "react";
import { ReactCase } from "./ReactCase";

export default {
  title: "React/ReactCase",
  component: ReactCase,
};

const ReactCaseLate = () => <ReactCase />;
export const ReactCaseLates = ReactCaseLate.bind({});
